{
  "name": "trader-tracker-aggregator",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production node ./dist/src/index.js",
    "build": "npm run build:clean && npm run build:tsc",
    "build:tsc": "tsc -p tsconfig.prod.json",
    "build:clean": "rm -r dist; exit 0"
  },
  "dependencies": {
    "@apollo/server": "^4.3.0",
    "cors": "^2.8.5",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  }
}
